# Insert properties with mock values that are necessary for the service context tests to run
spring:
  application:
    name: hi-filetransfers

file-server:
  username: mock
  password: mock
  host: mock
  remoteBaseDir: hi
  localDirPathTo: ./resources/tmp/to
  localDirPathFrom: ./resources/tmp/from

localdirpath:
  toPath: ./resources/tmp/to
  fromPath: ./resources/tmp/from