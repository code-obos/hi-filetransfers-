# Local development values would go here
logging:
  config: src/test/resources/logback-test.xml
  level:
    web: INFO
    no.obos: DEBUG

services:
  spring-boot-admin:
    #No URL disables SBA
    url:

localDirPathTo: ./tmp/in
localDirPathFrom: ./tmp/from

file-server:
  username: ${AS400_USERNAME}
  password: ${AS400_PASSWORD}
  host: obos.obos.no
  remoteBaseDir: hi
  localDirPathTo: ${localDirPathTo}
  localDirPathFrom: ${localDirPathFrom}


localdirpath:
  toPath: ${localDirPathTo}
  fromPath: ${localDirPathFrom}
